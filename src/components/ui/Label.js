import React from "react";
function Label({ title }) {
  return (
    <div>
      <label className="text-gray-800 font-Jost text-xl font-bold">{title}</label>
    </div>
  );
}

export default Label;
